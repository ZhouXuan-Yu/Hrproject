创建岗位问题：

1. 多级审批能实现，但是没有做身份校验，传对 level 就能进行审批，比如我用管理员的身份审批三次就可以，审批通过，通过后会显示通过时间。，但是不应该在审批进度/招聘进展那里显示，查看详情里面已经有了

2. 没有在操作中加删除按钮，用来删除岗位，现在好像是只允许删除草稿状态的   

3. 现在有一个问题就是之前的流程是创建岗位，职责什么的随便写，然后审批通过了，在去JD草稿生成里面自己再填岗位什么的，ai再扩展解析，hr他在把内容复制到那个招聘软件，但是我觉得可以把招聘辅助中
心的JD草稿生成，封装成一个api，然后在岗位查看详情的岗位描述后面加一个AI辅助分析，在这里调用api,在这里直接更新岗位描述，就不用去JD草稿生成中再输入了。在招聘辅助中心的JD草稿生成中加一个"关联需求"下拉，选中的话生成完扩展解析可以选择写入对应需求。（但现在有一个问题是生成的内容不能修改）
我做了：把 JD 生成能力统一到一个引擎（_run_jd_generate），然后两个入口共享它：

需求详情页 → AI 解析辅助：在岗位描述区域加了按钮，点击后弹出抽屉，自动带入该需求的岗位名称、部门、已有草稿。HR 只需补充业务背景（如技术栈、团队情况），AI 基于这些真实信息生成 JD，预览确认后直接写入当前需求，不用跳转、不用重新输入。
JD 草稿生成 → 关联需求：在输入区顶部加了需求下拉框，选中后自动填入岗位名称、部门、已有 JD 草稿，HR 补充修改后生成，结果下方可一键写回对应需求。
输出质量优化：重写了系统提示词，明确告诉 AI"这是内部审批文档不是招聘广告"，禁止营销套话；结构从四层标题压到两层，每条一句话；前端用 AiMarkdown 组件渲染，不再显示裸 ## 符号；抽屉里默认预览模式，可切换编辑。


4. 需求详情中的候选人，查看详情按钮没用，联系我觉得用处也没体现，

5. 有一个逻辑问题就是，面试轮次，第二轮应该是第一轮结果出来再说



简历入库，人岗匹配问题

1. 人才库硬规则打画像分不行
2. 如果是多个邮箱同时配置，系统能识别出来应该用哪个邮箱发送面试和录用offer吗，是不是应该设置一个专门的专门的选项，看用途是什么，我现在在招聘基础配置里面的编辑邮箱中添加了一个选项（已做），我发现我设置了哪个邮箱的收发状态，但是发送邮件的邮箱并没有按这个状态来
3. 草稿硬编码不行，内容不太合适，不正式
4. 面试通过有一个审批，谁审批，几个人，没看到有审批的地方
5. 没有入职包这个
6. 没有布置到公网上，所以只测试了发送邮件没有测试是不是能够确认
7. 通知模板那里的和硬编码的哪个优先级高，这一块没有真实数据呢
8. 刷新邮箱简历，出现同步已开始，请稍后刷新查看结果，但是页面没有更新，你看一下你的，刷新出来的简历感觉很拥挤，一行看着
9. 简历入库后再新建岗位就不能把人才库里面的简历匹配推荐到这个岗位中了，应该至少手动的时候能更新，现在手动都不行，检索人才库的功能需要实现，看一下match_service.py:90 — 匹配算法是不是假的，本质上还是匹配问题，有时候能匹配，有时候不能，什么都匹配不到，甚至入库也不能，点击重新匹配都不能
10. 撤回该条需求没什么用，我的建议是删除按钮
11. 用的是 MySQL，还是 SQLite 文件。我这边显示的是在本地用 SQLite 临时搭建验证环境，是不是要用的是连自己的 MySQL 数据库，我记得连 MySQL 数据库是连用户名密码啥的都行
12. 是不是IMAP 同步只拉取"未读"邮件。 你的 QQ 邮箱里的简历邮件如果已经被读过（哪怕你在手机 QQ 邮箱里点开过），就不会被同步。我验证的方法是没有 UID 追踪字段。需要两步修复：加列 + 改同步逻辑




